# Roadmap: site institucional Toroid do Brasil

Atualizado em 2026-08-10, a partir do estado real do repositório (`git log`, árvore de arquivos, `lib/wordpress.ts`, TODOs no código e a auditoria de design de 2026-08-05 em `.impeccable/critique/`). Ponto de partida para realinhar com `CLAUDE.md` e `PRODUCT.md` quando o projeto avançar de fase; não substitui esses dois arquivos, que continuam sendo a autoridade de convenção e posicionamento.

---

## 1. Onde estamos agora

### 1.1 Fundação técnica (concluída)

- Next.js App Router, TypeScript, Server Components por padrão.
- `next.config.ts`: `remotePatterns` de imagem para `toroid.com.br` (ainda não validado contra uma URL real de mídia), headers de cache para assets estáticos, 64 redirects 301 (na prática 308, ver nota abaixo) mapeados a partir do export completo do Search Console (`paginasGSC.md`, 83 URLs): as 3 URLs de produto que viveram em `/produtos/...`, o `__trashed` do WordPress antigo, 58 posts de blog (incluindo o typo `/tranformadores-de-tensao` e a versão com escrita correta, redirecionadas para o mesmo destino), um padrão de path para arquivos de data (`/:ano/:mes/:dia` → `/blog`), e `/whatsapp` → `/#orcamento`. Essencialmente completo — o que resta fora do mapa é por decisão explícita (`/lp` descontinuada, `/isobox` mantém o mesmo slug e por isso nunca precisou de redirect, PDFs seguem na Media Library do WP) ou pendência externa (política de privacidade), não por falta de dado.
- Nota técnica: `permanent: true` no Next.js retorna HTTP 308, não 301 — é o substituto moderno que preserva o método HTTP; Search Console/Google tratam como equivalente a 301 para sinal de ranking. Não é bug, é o comportamento padrão do framework desde os primeiros 2 redirects do projeto.
- Fontes via `next/font/google` (Montserrat nos títulos, Karla no corpo), sem `<link>` bloqueante.
- Sistema de design: tokens de cor em `globals.css`, ilustrações SVG próprias e determinísticas (`HeroToroid`, `CurrentWave`), zero bibliotecas pesadas.
- Regras de marca sendo aplicadas de fato, não só documentadas: "32 anos" correto em três componentes independentes, amarelo institucional usado uma única vez (Footer), com comentário no código marcando isso como intencional.

### 1.2 Páginas e conteúdo

| Página | Status | Observação |
|---|---|---|
| Home (`/`) | Construída | Hero, ProvaInstitucional, Produtos, Segmentos, Performance, Fabrica, CTA |
| `/transformador-de-corrente` | Construída, validada pela engenharia (2026-09-01) | Conteúdo estático, com tabela de especificação. URL top-level, exigida por tráfego/Ads já ativos (não é mais `/produtos/...`). Valores técnicos confirmados pela engenharia; ganhou a linha "Corrente secundária (1 A ou 5 A)" na tabela e o burden (VA) no checklist, que foi o único apontamento da revisão. Também ganhou seção de tipos construtivos, aviso de secundário aberto e FAQ visível (ver Trilha A, item 16). |
| `/transformador-de-potencia` | Construída (2026-08-12), validada pela engenharia (2026-09-01) | Nova família de produto (ver Trilha A, item 12), substitui Transformadores Toroidais na listagem de 3 famílias. Conteúdo estático, com tabela de especificação, valores herdados da antiga página de Transformadores Toroidais e agora confirmados pela engenharia, sem apontamento de correção. Ganhou autotransformador/isolador, blindagem, aviso de secundário em curto e FAQ visível (ver Trilha A, item 16). |
| `/transformadores-toroidais` | Construída, reframe (2026-08-12) | Deixou de ser uma das 3 famílias — agora é a página que explica a tecnologia de núcleo toroidal e compara com o convencional. Conteúdo estático, sem tabela de especificação própria (ver Trilha A, item 12). |
| `/indutores-filtros-e-chokes` | Construída (2026-09-01), sem tabela | O brief de conteúdo da engenharia chegou e foi aplicado: definição de indutor e indutância, leis de Faraday e Lenz, campo de aplicação (fontes, filtros, ressonância, DC-DC, chokes, RF), modo comum e modo diferencial, cuidados de aplicação e FAQ visível. A página deixou de ser a versão curta das três. **Continua sem tabela de especificação**: o brief é de conteúdo, não traz faixa de indutância/corrente/frequência confirmada. Marcado no próprio código. Slug diferente do nome interno de propósito, é a URL que já rankeia no WordPress antigo (37 cliques). |
| `/produtos` | Construída | Página de listagem das três famílias, reaproveitando `components/sections/Produtos.tsx` e `lib/produtos.ts`. |
| `/aplicacoes/nobreaks` | Construída, com ressalva | Primeira página de aplicação/segmento. Migrada de `/transformadores-nobreaks` em 2026-08-17 (301 em `next.config.ts`) para entrar no padrão de URL de aplicação do CLAUDE.md — continua estática, conteúdo próprio, não vem do CPT `aplicacao`. Valores técnicos (faixa de tensão, eficiência, regulação) vêm do brand book e **ainda não têm validação formal da engenharia** nem aprovação comercial da mensagem-chave. Está marcada `[PROPOSTA: VALIDAR COM COMERCIAL]` no próprio arquivo. |
| `/aplicacoes/[slug]` (padrão dinâmico do CLAUDE.md) | Construída (2026-08-12) | Consome `lib/wordpress.ts` (`getAplicacaoPorSlug`+`getProdutosPorIds`, uma única chamada batched). Sem `generateStaticParams`: renderiza por request, pra não depender de `WP_API_URL` no build. Sem `WP_API_URL`, devolve 404 controlado (`notFound()`), não erro 500. `/aplicacoes/nobreaks` é rota irmã estática dentro da mesma pasta, fora deste padrão dinâmico (decisão separada, ver aviso no próprio arquivo) — Next.js prioriza a rota estática sobre `[slug]`, sem conflito. |
| `/blog/[slug]` + listagem `/blog` | Construída (2026-08-12), bug de clique corrigido (2026-08-25) | Mesmo padrão acima, consumindo `getPosts`/`getPostPorSlug`. Sem `WP_API_URL`, a listagem mostra estado vazio ("Nenhum conteúdo publicado ainda") e o slug individual devolve 404 controlado — nenhum dos dois quebra o build nem retorna 500. **Bug reportado 2026-08-25**: com o WP já puxando os posts reais em produção, os cards da listagem não abriam ao clicar. Causa: `app/blog/page.module.css` tinha hover (elevação, sombra) no `<article className={styles.card}>` inteiro, mas o `<Link>` real só envolvia o texto do título, então clicar no resumo ou no espaço em branco do card não navegava. Corrigido em `app/blog/page.tsx`/`page.module.css`: o `<Link>` agora envolve o card inteiro. |
| Formulário de orçamento | Construído | `components/forms/OrcamentoForm.tsx` + `/api/orcamento`, com persistência em MongoDB e e-mail de notificação em paralelo, captura de UTM first/last-touch. Ver seção "Formulário de orçamento e captura de lead" em CLAUDE.md. Esta linha estava desatualizada. |

### 1.3 Integração com WordPress: confirmada em produção para posts de blog; produto/aplicação ainda não

**Atualização 2026-08-17: `WP_API_URL` confirmado funcionando em produção** — `/blog` em `toroid-website.vercel.app` já renderiza os posts reais do WordPress (20 títulos verificados ao vivo, sem estado vazio). `app/sitemap.ts` atualizado no mesmo dia para incluir `/blog/[slug]` de cada post (bloco que ficava comentado esperando exatamente essa confirmação, com fallback `.catch(() => [])` para não derrubar o sitemap inteiro numa instabilidade pontual do WP).

`lib/wordpress.ts` tem tipos completos (`Produto`, `Aplicacao`, `Post`), mapeamento do formato bruto da REST API para o formato interno, e funções de fetch com `revalidate`/`tags` seguindo a convenção do CLAUDE.md. **Blog é o primeiro conteúdo real ligado.** Produto e Aplicação continuam sem uso end-to-end: as três páginas de produto e a página de nobreaks têm conteúdo estático, porque os CPTs `produto`/`aplicacao` ainda devolvem 404 na REST API (diagnóstico de 2026-08-12, ver Trilha B — provavelmente "Show in REST API" desligado no registro do CPT, distinto da confirmação de `WP_API_URL` em si, que já está ok). Isso continua sendo o maior ponto de alavancagem restante: a camada está pronta e uma fatia dela (posts) já provou que funciona de ponta a ponta; falta só os outros dois CPTs serem expostos do lado do WP.

### 1.4 O que a auditoria de design já encontrou (2026-08-05)

Rodada única de `/impeccable` sobre a home. Cruzando com o estado atual do código:

| Item | Status agora |
|---|---|
| Páginas de produto retornavam 404 | **Resolvido** (as 3 páginas existem hoje) |
| Dropdown "Produtos" do Nav não abria em telas touch | **Resolvido** (hoje é um `<button onClick>`, não mais só `:hover`) |
| Gradiente institucional atrás de texto no card de CTA | **Resolvido** (comentário no CSS confirma o ajuste: "nenhuma faixa clara passa atrás do texto") |
| Nenhum mecanismo real de captura de lead (P0) | **Ainda aberto** |
| Três instâncias de texto branco sobre fundo branco (contraste 1:1) e tokens de texto abaixo de AA | **Não verificado nesta passagem**, provavelmente ainda aberto |
| Seção Performance lida como argumento de "toroidal é superior" | **Não verificado nesta passagem**, provavelmente ainda aberto |
| Placeholder `[CASE PENDENTE: aguardando aprovação comercial]` renderiza literal para o visitante | **Ainda aberto** (é um placeholder deliberado, mas com redação de ticket, não de visitante) |

---

## 2. As duas trilhas de trabalho que faltam

### Trilha A: desenvolvimento (não depende de ninguém fora do repositório)

1. ~~Formulário de orçamento técnico + API Route de back-end~~ **Concluído.** Validação por tipo de produto (TC / TP / Indutor) continua pendente de decisão comercial para reativar os campos completos já escritos (comentados) em `lib/orcamento-schema.ts`.
2. ~~Corrigir os itens de acessibilidade/contraste ainda abertos da auditoria~~ **Concluído** (2026-08-12). Achado real, confirmado com o detector do `/impeccable` contra o servidor de dev: a legenda das fotos do mosaico em `Fabrica.tsx` (`tileCaption`) podia cair para contraste quase invisível, porque o degradê de proteção (`tileOverlay`) já estava quase transparente antes do topo de uma legenda de 3 linhas, e o placeholder por trás (`.tile`, antes de a foto carregar ou se ela falhar) era um tom claro. Corrigido em `Fabrica.module.css`: degradê mantém opacidade alta por mais tempo e o placeholder passou a ser azul escuro, não claro. O outro achado do detector (branco sobre verde claro nos links do Nav) foi investigado e é falso positivo confirmado no próprio código do detector: ele lê o `background-image` do sublinhado animado de `.navLink` (dimensionado a 2px, 0% em repouso) como se cobrisse o link inteiro. Sem bug real ali, nenhuma mudança de código.
3. ~~Reescrever o placeholder de case (`Segmentos.tsx`)~~ **Concluído** (2026-08-12). `[CASE PENDENTE: aguardando aprovação comercial]` virou "Estamos reunindo os primeiros cases de cliente. Em breve, aqui." Mesmo estado de fundo (ainda sem case liberado, ver `PRODUCT.md`), só a redação passou a ser voltada ao visitante. **Atualização (2026-08-25)**: esse card (que por essa altura já tinha virado só "Prova social / Em breve", uma versão ainda mais curta do mesmo placeholder) foi removido do grid, e o grid passou de `display: grid` (colunas fixas) para flexbox com `justify-content: center`, pra centralizar a última linha em vez de deixar 5 cards com uma célula vazia. No lugar, `components/sections/ProvaGoogle.tsx` (nova seção pequena, fora do grid de segmentos): nota real da Toroid no Google (4,6, 78 avaliações, confirmado pelo usuário via captura de tela do perfil, link oficial `share.google/...`), estrelas em azul institucional (não no dourado do selo do Google, por causa da regra de amarelo exclusivo do logo em CLAUDE.md). Ligada em `app/page.tsx`, entre `Segmentos` e `Performance`.
4. ~~Revisar a seção Performance para não posicionar a tecnologia toroidal como superior às demais~~ **Concluído** (2026-08-12). `Performance.tsx`: eyebrow trocado de "Performance técnica" para "Performance do núcleo toroidal", e novo texto de apoio deixa explícito que as características vêm da geometria toroidal especificamente, e que transformadores de corrente e indutores seguem princípios próprios. Nenhum dado técnico dos 4 itens de benefício foi alterado, só o enquadramento da seção.
5. ~~Construir o padrão dinâmico `/aplicacoes/[slug]`~~ **Concluído** (2026-08-12). `app/aplicacoes/[slug]/page.tsx`, consumindo `getAplicacaoPorSlug`+`getProdutosPorIds` de `lib/wordpress.ts` (relacionamento produto↔aplicação resolvido numa única chamada batched, regra do CLAUDE.md). Sem `generateStaticParams` de propósito — gerar params no build chamaria a API com `WP_API_URL` ainda não confirmado e quebraria `next build`; cada slug renderiza por request. `AplicacaoHero.arte` virou prop opcional (o CPT `aplicacao` não tem campo de arte customizado). `/transformadores-nobreaks` não foi migrada pra esse padrão — continua estática, decisão futura separada.
6. ~~Construir `/blog/[slug]` e a listagem de blog~~ **Concluído** (2026-08-12). `app/blog/page.tsx` (listagem, `getPosts()`) e `app/blog/[slug]/page.tsx` (`getPostPorSlug`), mesmo padrão sem `generateStaticParams` do item acima. **Isso não faz os 58+ redirects 301/308 do mapa de URLs pararem de 404 sozinho**: a rota agora existe e responde com `notFound()` controlado (não 500) quando `WP_API_URL` está ausente ou o post não existe — mas o 404 só desaparece de fato pro visitante quando `WP_API_URL` for confirmado E os posts existirem no WP com os mesmos slugs (Trilha B). Rota existir ≠ conteúdo publicado. **Atualização (2026-08-12): os posts já existem** — ver diagnóstico real na Trilha B, seção 2.
7. ~~`sitemap.ts` e `robots.ts`~~ **Concluído** (Parte 5): `app/sitemap.ts` gera dinamicamente as 7 rotas reais hoje (bloco de posts do WP comentado, pronto para ativar quando `/blog/[slug]` existir e `WP_API_URL` estiver validado — não antes, pra não anunciar URL que ainda dá 404). `app/robots.ts` bloqueia `/wp-admin` e `/wp-json`, aponta pro sitemap, e já aplica a política de crawler de IA da diretoria (permitir bot de busca/resposta, bloquear bot de treinamento).
8. ~~Integração de GA4~~ **Concluído** (Parte 6): `components/analytics/GoogleAnalytics.tsx` carrega `gtag.js` condicionado a `GA4_MEASUREMENT_ID` existir (sem a env var real, não renderiza nada — mesmo padrão de degradação do MongoDB/SMTP). Dois eventos implementados em `lib/analytics.ts`: `whatsapp_click` (dispara em todo link de WhatsApp do site, centralizado em `components/analytics/WhatsAppLink.tsx`, que substituiu 6 ocorrências de `<a href={whatsappLink}>` espalhadas em `CTA.tsx`, `WhatsAppButton.tsx`, `AplicacaoHero.tsx`, `Hero.tsx`, `Pillar.tsx` e `transformadores-nobreaks/page.tsx`) e `form_submit` (dispara em `OrcamentoForm.tsx` só após confirmação real do servidor). Os dois anexam o UTM de último toque de `lib/attribution.ts` — mesma fonte que já alimenta o lead no MongoDB, então Ads/GA4/CRM comparam o mesmo dado de atribuição. **Verificação real no DebugView ainda depende de `GA4_MEASUREMENT_ID` real (Trilha B) — só testei a estrutura do código (script carrega/não carrega conforme a env var, eventos disparam com o parâmetro certo), não uma sessão de GA4 de verdade.**
9. ~~API Route de revalidação sob demanda (`revalidateTag`) para o webhook do WordPress~~ **Concluído** (2026-08-12). `app/api/revalidate/route.ts`, autenticado por `REVALIDATE_SECRET` (novo env var, ver CLAUDE.md), mapeia `{post_type, slug}` para as mesmas tags de `lib/wordpress.ts`. Falta só o WordPress chamar essa rota (Trilha B).
10. Trocar o conteúdo estático das páginas de produto/aplicação por `lib/wordpress.ts` assim que a Trilha B confirmar o endpoint.
11. ~~Dados estruturados (JSON-LD): `Product`/`BreadcrumbList`/`FAQPage` em todas as páginas de produto e `BreadcrumbList`/`CollectionPage` na listagem `/produtos`~~ **Concluído.** Já existiam antes da Parte 5, usando specs técnicas reais de cada página — nada novo precisou ser escrito aqui. **Parte 5**: `Organization` (`components/seo/OrganizationSchema.tsx`) montado uma única vez no `RootLayout`, aplicado em todo o site — extraído do que já existia isolado em `/contato` (que tinha um bloco quase idêntico e duplicado, removido). **2026-08-13**: `LocalBusiness` construído (`components/seo/LocalBusinessSchema.tsx`, montado só em `/quem-somos` — endereço, telefone e horário de funcionamento seg. a sex. 7h30–17h30) e `FAQPage` construído na mesma página (`components/quem-somos/FAQSection.tsx`, reaproveitando `components/aplicacoes/Perguntas.tsx`). Falta ainda: `FAQPage` no Pilar 7, quando essa página existir (é conteúdo diferente, sobre autoridade técnica, não institucional). Convenção documentada em CLAUDE.md.
12. ~~Corrigir a taxonomia de produto: "Transformadores Toroidais" não é uma família, é tecnologia de núcleo cross-line~~ **Concluído** (2026-08-12). Duas correções: (a) a página de TC citava "linha CX01, CX02 e CX03" em 4 lugares como se fosse uma linha real da Toroid — não é, reescrito de forma genérica ("TC compacto para painel", sem nome de linha inventado). (b) a listagem de 3 famílias (`lib/produtos.ts`, que alimenta Nav/home/`/produtos`) tinha Transformadores de Corrente / **Transformadores Toroidais** / Indutores como se fossem coordenadas — mas núcleo toroidal é tecnologia aplicável a qualquer uma das três, não uma família própria (as próprias páginas de TC e Indutores já reconhecem isso com seções "núcleo toroidal ou convencional"). Corrigido: a família que faltava era **Transformador de Potência** (slug já reservado no ROADMAP desde antes, `/transformador-de-potencia`), agora construída como página completa (hero/specs/checklist, mesmo nível de profundidade de TC/Indutores) substituindo Toroidal na listagem. `/transformadores-toroidais` manteve a URL (já indexada) mas mudou de papel: virou a página que explica a tecnologia toroidal e compara com o convencional, sem tabela de especificação própria (essa tabela migrou pra TP) e sem schema `Product` isolado (mantém `BreadcrumbList`+`FAQPage`). Arquivos tocados: `lib/produtos.ts`, `components/ui/ProductIcons.tsx`, `components/produtos/PillarEmblem.tsx` (novo ícone/emblema "potencia"), `app/transformador-de-potencia/page.tsx` (novo), `app/transformadores-toroidais/page.tsx` (reframe), `app/transformador-de-corrente/page.tsx` (CX0x), `Footer.tsx`, `app/sitemap.ts`, `PRODUCT.md`, `CLAUDE.md`. Pendência gerada por essa mudança: taxonomia `categoria_produto` do CPT `produto` no WordPress ainda tem o enum antigo (`transformador-toroidal` em vez de `transformador-de-potencia`) — ver Trilha B.
13. ~~Ajustar o Nav para o novo formato (Produtos, Sobre, Blog, Contato)~~ **Concluído** (2026-08-13). `components/layout/Nav.tsx`: removido o item "Segmentos" (a seção `Segmentos` continua existindo na home — só deixou de ter link direto no nav) e adicionado "Blog", apontando para a rota real `/blog` (já construída no item A.6, não é âncora). Nova ordem, igual no desktop e no mobile: Produtos (dropdown) → Sobre → Blog (`/blog`) → Contato (`/contato`). **Atualização (2026-08-13, mesmo dia): `/quem-somos` foi construída (ver item A.11) e `Sobre` passou de âncora `/#fabrica` para rota real `/quem-somos`**, no desktop e no mobile.
14. ~~Construir `/quem-somos`~~ **Concluído** (2026-08-13). Conteúdo (história, missão, visão, valores, política da qualidade) copiado do WordPress publicado (`https://toroid.com.br/quem-somos/`, via REST API) e conferido contra o brand book do Notion (`00-visao-missao-valores.md`) — os 4 valores oficiais (Comprometimento, Respeito, Reconhecimento, Família), a missão ("Transformando energia e vidas.") e a visão batem entre as duas fontes. "30 anos" do WP trocado por `getAnosDeMercado()` (32 anos hoje, nunca hardcoded, ver CLAUDE.md). A origem sueca do grupo aparece só como contexto histórico (frase literal aprovada no brand book), não como diferencial comercial. Layout deliberadamente diferente das demais páginas, a pedido: hero azul cheio com `position: sticky` e a seção seguinte (`Historia`) sobe por cima dele no fluxo normal do documento — efeito de cartão deslizando sobre o azul, sem nenhum JS de scroll (ver comentário em `components/quem-somos/QuemSomosHero.module.css`). A frase-síntese da marca ("Transformando energia e vidas.") ganha destaque em `components/quem-somos/MissaoDestaque.tsx`, com texto em gradiente animado (única ocorrência desse tratamento no site, guardada por `prefers-reduced-motion`). Arquivos novos: `app/quem-somos/page.tsx`, `components/quem-somos/*` (`QuemSomosHero`, `Historia`, `MissaoDestaque`, `Valores`, `Compromissos`, `FAQSection`), `components/seo/LocalBusinessSchema.tsx`. Imagens reaproveitadas de `public/images` (`fachada-placa.webp`, `fabrica-interna.webp`, selos de certificação) — nenhuma imagem nova, sem depender de mídia do WordPress ainda não validada (ver item Trilha B sobre `WP_API_URL`). `app/sitemap.ts` atualizado com a nova rota.
15. ~~Migrar `/transformadores-nobreaks` para o padrão de URL `/aplicacoes/*`~~ **Concluído** (2026-08-17). Pasta movida para `app/aplicacoes/nobreaks/page.tsx` (mesmo conteúdo estático, mesma estrutura — só a URL mudou), canonical e URLs do JSON-LD (`Product`/`BreadcrumbList`) atualizadas para `/aplicacoes/nobreaks`, 301 de `/transformadores-nobreaks` adicionado em `next.config.ts`, e `app/sitemap.ts` atualizado com a nova rota. É uma rota irmã estática dentro de `app/aplicacoes/`, ao lado de `[slug]/page.tsx` (que é 100% dado do CPT `aplicacao`, ainda bloqueado — ver Trilha B) — Next.js prioriza a rota estática sobre a dinâmica no mesmo nível, então convivem sem conflito. Trocar o conteúdo estático por `lib/wordpress.ts` continua sendo decisão futura separada, sem depender desta migração de URL.

16. ~~Aplicar o conteúdo técnico revisado e aprovado pela engenharia nas três páginas de família~~ **Concluído** (2026-09-01). A engenharia revisou o conteúdo publicado e entregou três documentos de referência (TC, TP, Indutor). Único apontamento de correção, sobre TC: a especificação final também define **corrente secundária** (1 A ou 5 A) e **carga nominal em VA (burden)** — a primeira virou linha da `PillarSpecTable` (e por consequência `additionalProperty` do `Product`), a segunda virou item de checklist, por não ter faixa única de linha para declarar. Isso fecha a pendência de "valores técnicos sem validação formal" em TC e TP; os comentários `ATENÇÃO, VERIFICAR ENGENHARIA` no topo desses dois arquivos viraram registro de validação (o de `/aplicacoes/nobreaks` **fica**, aqueles números não estavam no escopo da revisão). Conteúdo novo publicado: **TC** ganhou relação de transformação com exemplo (100/5 A), princípio de Faraday, as duas categorias da NBR 6856 com a diferença de tempo de resposta, definição de saturação, aviso de secundário aberto e seção de tipos construtivos (janela, bipartido, barra, bucha, enrolado); **TP** ganhou elevador/abaixador no SEP, o limiar de 1000 V da NR-10, autotransformador contra transformador isolador, blindagem eletrostática contra magnética e aviso de secundário em curto; **Indutores** deixou de ser a página curta das três, com definição de indutor e indutância (henry), leis de Faraday e Lenz, campo de aplicação (fontes, filtros, ressonância, DC-DC, chokes, RF), modo comum e modo diferencial, cuidados de aplicação e o `PillarClosing` que estava comentado. Mudanças transversais: as três ganharam **seção de FAQ visível** (`components/aplicacoes/Perguntas.tsx`, que só existia em `/aplicacoes/*` e `/quem-somos`) — antes o `FAQPage` existia só no JSON-LD, sem texto publicado — e o `mainEntity` passou a ser derivado da constante `PERGUNTAS`, como `ESPECIFICACOES` já alimentava o `additionalProperty`, para o dado estruturado não poder divergir do texto visível; os `BreadcrumbList` que hardcodavam `https://toroid.com.br` passaram a usar `absoluteUrl()`, como o CLAUDE.md já exigia. Componente novo: **`PillarAviso`** em `components/produtos/Pillar.tsx` (primeiro bloco de alerta do design system, em azul institucional e não em cor de warning, já que o amarelo é proibido em UI), usado três vezes. **Decisão de conteúdo**: o documento de TP compara núcleo EI com núcleo toroidal e conclui pela superioridade do toroidal; essa parte **não foi publicada em lugar nenhum**, porque o CLAUDE.md proíbe a formulação e `/transformadores-toroidais` proíbe a comparação — está marcado em comentário no topo de `app/transformador-de-potencia/page.tsx` para não ser reintroduzida. Por isso `/transformadores-toroidais` não foi alterada. Tudo replicado nas gêmeas em `app/es/`. Pendência gerada: confirmar com o comercial quais tipos construtivos de TC a Toroid fornece além de janela e bipartido (ver Trilha B).

### Trilha B: confirmações e configurações externas (dependem de outras pessoas ou sistemas)

| Pendência | Quem resolve | Bloqueia o quê |
|---|---|---|
| ~~Validar `WP_API_URL` real~~ **Resolvido para posts (2026-08-17)**: confirmado funcionando em produção, `/blog` em `toroid-website.vercel.app` já mostra os posts reais do WP. **Ainda pendente para Produto/Aplicação — diagnóstico corrigido em 2026-08-17**: `wp/v2/produto` e `wp/v2/aplicacao` continuam retornando 404, e nem aparecem em `wp/v2/types` — os dois CPTs não estão registrados com REST habilitado. **A causa não é "Custom Post Type UI"** (esse plugin não está instalado no site — corrigido também no CLAUDE.md); via `wpcom-mcp-plugin-management` confirmei que o plugin real é **Pods — Custom Content Types and Fields** (v3.3.9.1, ativo). O namespace `pods/v1` nem aparece em `wp-json/` — sinal de que o componente REST API do Pods está desligado, além do toggle por pod. **Passo a passo pra quem tiver acesso ao wp-admin:** (1) `Pods Admin → Settings → Components` → confirmar/ativar o componente "REST API" do Pods, se existir essa etapa nesta versão; (2) `Pods Admin → Edit Pods → produto` → aba "Advanced Options" → sub-aba "REST API" → marcar a opção de habilitar leitura/escrita via REST (o texto exato varia por versão: "Enable read/write access via the REST API" ou "Show in REST API") → Save; (3) repetir o passo 2 para o pod `aplicacao`; (4) validar com `curl -s https://toroid.com.br/wp-json/wp/v2/produto/123 \| jq` e confirmar que os campos ACF aparecem no JSON (CLAUDE.md). Não consegui aplicar isso eu mesmo: a escrita de configurações via MCP (`settings.update`) está desabilitada nas permissões desta conexão, e não existe uma ferramenta MCP dedicada ao Pods — precisa ser feito no wp-admin mesmo. **Decisão consciente (2026-08-17): quem tem acesso ao wp-admin optou por não aplicar essa mudança agora**, porque o WordPress atual ainda hospeda o site *em produção* (o site novo em Next.js ainda não foi aprovado/está em staging) — mexer na configuração REST do Pods nesse momento é risco desnecessário sobre o site que está ao vivo, não sobre o que está em desenvolvimento. Não é mais pendência de terceiro sem resposta; é adiamento deliberado até a aprovação/corte de DNS (ver linha "Plano de corte (DNS/domínio)" abaixo). | Quem tem acesso ao WP admin — adiado por decisão própria até a aprovação do site novo | Trocar o conteúdo estático das páginas de produto/aplicação por `lib/wordpress.ts` (item A.10) |
| Configurar webhook no WordPress para chamar a rota de revalidação ao publicar/editar — rota já existe (`/api/revalidate`, ver item A.9 acima e CLAUDE.md), falta só o lado do WordPress. | Quem tem acesso ao WP admin | Conteúdo fresco sem depender de rebuild manual |
| ~~Confirmar telefones e e-mail oficiais~~ **Já resolvido, esta linha estava desatualizada** — `Footer.tsx`/`ContatoInfo.tsx` já têm dado real (não placeholder) há algum tempo; re-confirmado diretamente na Parte 5 (telefones (41) 3035-8282/8258, endereço com bairro Iná, agora também no schema `Organization`). | — | — |
| ~~Confirmar número real de WhatsApp~~ **Já resolvido, esta linha estava desatualizada** — `lib/whatsapp.ts` já marca "Número confirmado" no próprio comentário, mesmo número reconfirmado na Parte 5. | — | — |
| ~~Confirmar URLs dos perfis oficiais~~ **Já resolvido, esta linha estava desatualizada** — `Footer.tsx`/`ContatoInfo.tsx` já linkam LinkedIn/Instagram/YouTube reais, não `href="#"`. | — | — |
| ~~Política de crawler de IA~~ **Resolvido**: `app/robots.ts` permite bot de busca/resposta que cita fonte (`OAI-SearchBot`, `Claude-SearchBot`, `Claude-User`, `PerplexityBot`, `Google-Extended`) e bloqueia bot de treinamento sem link de volta (`GPTBot`, `ClaudeBot`). | — | — |
| ~~Criar propriedade GA4 e confirmar `GA4_MEASUREMENT_ID`~~ **Resolvido** — já existia um valor real (`G-XFVM9FCZET`), só não estava propagado pra Preview/Production. Corrigido em 2026-08-13. | — | — |
| ~~Configurar as variáveis de ambiente no Vercel (Production, Preview e Development)~~ **Resolvido em 2026-08-13**, via `vercel env ls`/`env add` confirmado diretamente no projeto (`toroid/toroid-website`): `WP_API_URL` (valor corrigido, estava sem `/wp/v2`), `WHATSAPP_NUMBER`, `GA4_MEASUREMENT_ID`, `REVALIDATE_SECRET` e `LEAD_IP_HASH_SECRET` (os dois últimos gerados agora) presentes nas três. **`MONGODB_URI` também resolvido em 2026-08-13** — cluster `cluster0-toroidsite` no Atlas, usuário `gabriel_db_user`, conexão testada e confirmada (ping ok, banco `toroid` já existe, ainda sem collections — normal, `leads` só é criada no primeiro insert real). Pendência real que resta: **nenhuma env var nova entra em vigor até o próximo redeploy** — o deployment de Production atual foi feito antes de qualquer uma dessas 6 variáveis existir lá. | — | — |
| ~~Exportar do Search Console o mapa completo de URLs indexadas do WP antigo~~ **Resolvido** — export completo recebido (`paginasGSC.md`, 83 URLs). Mapa de redirects 301/308 em `next.config.ts` agora tem 64 entradas cobrindo tudo que tinha destino claro. | — | — |
| ~~Construir as páginas institucionais que ainda não existem no Next.js, mantendo o mesmo slug~~ **Atualizado (2026-08-25)**: `/trabalhe-conosco` construída (conteúdo real: e-mail de RH e Instagram, o WP antigo também só tinha isso, sem vaga publicada nem processo seletivo formalizado). `/suporte-especializado-toroid` **não é uma página institucional**: diagnóstico corrigido, é um post nativo do WP (`wp/v2/posts?slug=suporte-especializado-toroid`, id 604), coberto pela integração de blog já confirmada (item 1.3) e só faltava o 301 dele em `next.config.ts` (adicionado, `/suporte-especializado-toroid` → `/blog/suporte-especializado-toroid`). `/politica-de-privacidade` **descartada por decisão** (não é mais pendência bloqueada, ver linha "Política de privacidade" abaixo). `/quem-somos` saiu desta lista: construída em 2026-08-13 (Trilha A, item 14). `/transformador-de-potencia` saiu desta lista: virou a 3ª família de produto (Trilha A, item 12). `/blog` (listagem) saiu desta lista pelo mesmo motivo, ver item A.6. | — | — |
| **Nova pendência (2026-08-12):** taxonomia `categoria_produto` do CPT `produto` no WordPress ainda usa o enum antigo (`transformador-de-corrente` \| `transformador-toroidal` \| `indutor-reator`, ver CLAUDE.md) — desde a correção de taxonomia do front-end (Trilha A, item 12), a família real é `transformador-de-potencia`, não `transformador-toroidal`. Decidir no WP admin: renomear o termo da taxonomia, ou criar `transformador-de-potencia` e decidir se `transformador-toroidal` continua existindo como tag secundária/cross-line (não como categoria). Sem isso, quando `WP_API_URL` for confirmado e o conteúdo estático for trocado por dados reais do WP (item A.10), o campo `categoria_produto` que chega da API vai contradizer a estrutura de 3 famílias do código. | Quem tem acesso ao WP admin | Consistência de dado quando a integração WP for ligada (item A.10) |
| ~~`/isobox`: mantém o slug (Grupo A), página nova a construir. Confirmado pela diretoria que não é prioridade agora.~~ **Resolvido (2026-09-04)**: o comercial antecipou a prioridade (apoio de copy do site novo, ver conversa) e pediu um card de produto para o Isobox na home apontando para essa rota. Página construída em `app/isobox/page.tsx` (+ espelho `app/es/isobox/page.tsx`), mesmo padrão das outras famílias (`PillarHero`/`PillarSpecTable`/`PillarChecklist`/`Perguntas`/`CTA`, JSON-LD `Product`+`BreadcrumbList`+`FAQPage`), com os dados técnicos confirmados na página equivalente do WordPress atual. | — | — |
| ~~`/lp`~~ **Resolvido**: página descontinuada, confirmado que não será reconstruída. Sem página, sem redirect, 404 natural aceito. | — | — |
| ~~`/tranformadores-de-tensao/` (typo)~~ **Resolvido**: é uma URL real e correta (apesar do typo), redireciona para `/blog/tranformadores-de-tensao`. A versão com escrita correta (`/transformadores-de-tensao/`), que hoje aponta por engano pra um arquivo de mídia do WP, também foi redirecionada defensivamente pro mesmo destino. | — | — |
| ~~PDFs técnicos indexados~~ **Resolvido**: seguem servidos direto da Media Library do WordPress (`Transf-corrente.pdf`, `Transf-Transistorizados.pdf`), sem migração nem redirect — o host de mídia do WP não sai do ar, só o front-end de páginas. | — | — |
| ~~Reestruturação de UTM da Toroid~~ **Escopo definido (2026-08-26)**: esta linha estava desatualizada — a convenção de nomenclatura (`plano-de-utm.md`, Notion) e o registro de campanhas (`matriz-utm.md`, Notion) já estavam marcados "Definido e vigente" desde 2026-08-21. Faltava aplicar essa convenção às páginas reais do site novo; isso foi feito nesta data, como uma seção "Matriz de referência rápida — página × canal" adicionada a `plano-de-utm.md`, cruzando LinkedIn/Instagram/YouTube (únicos 3 canais reais, confirmados em `Footer.tsx`/`ContatoInfo.tsx`) com as 16 páginas reais do site, mais um checklist de go-live. De caminho, corrigido também um bug relacionado em `components/analytics/WhatsAppLink.tsx`: a mensagem de WhatsApp só embutia `utm_campaign` na origem do lead e descartava `utm_source`/`utm_medium` — o dado que de fato identifica o canal. Os links de campanha legados do WordPress (`/whatsapp-linkedin/`, `/whatsapp-google/`, `/fabrica-de-transformadores-linkedin/`) continuam de fora do mapa de redirect (404 aceito), substituídos por esta matriz. | — | Pendente só a execução: aplicar os links da matriz nos 3 perfis sociais e nas campanhas Ads ativas depois do corte de DNS — ver checklist em `plano-de-utm.md` (Notion) |
| ~~`/blog/[slug]` e a listagem `/blog` só param de 404 quando WP_API_URL for confirmado E os posts existirem no WP~~ **Resolvido (2026-08-17)**: `WP_API_URL` confirmado em produção, `/blog` já mostra os posts reais (verificado ao vivo, 20 títulos). Diagnóstico anterior (2026-08-12) via `curl` já mostrava **64 posts nativos publicados no WP**, corpo `<p>` simples colado do Word (não Elementor), 3 com frase banida pelo tom de voz e 13 com resíduo de paste do Word — este último **corrigido de vez no código**: `limparResiduoDoWord()` em `lib/wordpress.ts` remove o wrapper automaticamente. **Pendência real que resta**: revisão manual pontual dos ~5 posts com problema de conteúdo (frase banida ou "30/40 anos" desatualizado) — não é mais bloqueio técnico, é revisão editorial. A própria URL `/blog/` já tinha 287 impressões no WP antigo. | Quem revisar o conteúdo de blog | Experiência real do visitante que seguir um link antigo |
| Dois posts de blog redirecionados têm "30 anos" no título/slug (`/30-anos-toroid-do-brasil`, `/mais-de-30-anos-de-lideranca-na-fabricacao-de-transformadores-e-indutores`) — dado desatualizado (são 32, nunca 40, ver CLAUDE.md). O redirect preserva só a URL; corrigir o texto é tarefa de quem revisar o conteúdo. | Quem revisar o conteúdo de blog | Consistência do dado institucional quando esses posts forem republicados |
| ~~Um par de posts de blog pode ser o mesmo artigo republicado com slug diferente~~ **Resolvido (2026-08-12)**: conferi o conteúdo real dos dois (`/como-os-transformadores-toroidais-otimizam-espaco-em-projetos-eletricos` e `/compactacao-inteligente-como-os-transformadores-toroidais-otimizam-espaco-em-projetos-eletricos`) — não são duplicados, são textos diferentes (748 vs. 2461 caracteres, aberturas diferentes), só o tema é parecido. Redirects separados continuam corretos como estão. | — | — |
| ~~Política de privacidade~~ **Resolvido por decisão (2026-08-25)**: diretoria optou por manter só o consentimento básico já implementado (banner de cookies, Google Consent Mode v2), sem desenvolver uma política de privacidade completa nem o checkbox de consentimento LGPD no formulário de orçamento. `consent.lgpd` permanece `false` por design permanente (`lib/leads-schema.ts`), não é mais pendência. | — | — |
| **Conflito de tipografia**: código e CLAUDE.md usam Montserrat (títulos) + Karla (corpo), decisão já implementada por peso de bytes (ver comentário em `app/layout.tsx`). O brand book oficial mais recente do Notion (`brand-book/03-identidade-visual.md`, 3 jul) diz "Montserrat em todas as aplicações" e não menciona Karla; uma cópia mais antiga do brand book descreve Raleway + Open Sans. Nenhuma fonte do Notion confirma Karla. | Quem for dono do brand book | Decidir se o código muda para Montserrat único, se o brand book precisa de atualização, ou se as duas fontes coexistem oficialmente |
| Validação formal da engenharia sobre os dados técnicos da página de nobreaks | Engenharia | Página de nobreaks ir ao ar com segurança jurídica/técnica. **Parcialmente resolvido em 2026-09-01**: a engenharia validou TC e TP, mas os números de nobreaks não estavam no escopo dessa revisão e continuam marcados no arquivo. |
| Faixa técnica de indutor confirmada (indutância, corrente, frequência da linha padrão) | Engenharia | Página de indutores ganhar a `PillarSpecTable` e o `additionalProperty` do schema `Product`. O brief de conteúdo chegou em 2026-09-01 e já foi aplicado; falta só a faixa numérica. |
| Confirmar quais tipos construtivos de TC a Toroid fornece além de janela e bipartido (barra, enrolado) | Comercial/Engenharia | A seção "Tipos construtivos" de `/transformador-de-corrente` declara hoje que a linha é construída em torno de janela e bipartida até 1,5 kV, e trata barra, bucha e enrolado como referência de especificação. Se a Toroid fornece barra ou enrolado, essa linha de escopo precisa ser ajustada. |
| Aprovação comercial da mensagem-chave da página de nobreaks (dor de campo vs. dor inferida da NBR5356-5) | Comercial | Mesmo item acima |
| Liberação de cases/depoimentos de cliente | Comercial | Substituir o placeholder de prova social por conteúdo real |
| Plano de corte (DNS/domínio) do WordPress atual para o Next.js na Vercel | Quem administra infraestrutura | Data de go-live |

### Mapeamento de URLs das campanhas Ads ativas para o novo site (Semana 2 do cronograma de execução)

Ponto de partida a partir dos dados reais de `plano-de-acao.md` (Notion, jun/2026) — **não é confirmação de final URL real na conta do Ads**, isso exige acesso ao painel (`ads.google.com`), que este repositório não tem. Serve para quem tiver esse acesso trocar a final URL de cada campanha pela página correspondente do site novo, sem precisar redescobrir o mapeamento do zero.

| Campanha | Página equivalente hoje no Next.js | Observação |
|---|---|---|
| Search (principal) — termos de marca ("toroid") | `/` (home) | Termo de marca, sem produto específico |
| Search (principal) — "transformador toroidal", "nucleo toroidal p/ transformador" | `/transformadores-toroidais` | Página de tecnologia de núcleo, não de família de produto (ver nota em CLAUDE.md) — é a que já rankeia organicamente para esse termo |
| Search (principal) — "trafo" (termo genérico) | `/produtos` | Sem produto específico na keyword, listagem das 3 famílias é o destino mais correto |
| DGen Transformadores | `/produtos` | Display genérico sobre transformadores, sem segmento — mesma lógica do item acima |
| DGen Refrigeração | `/produtos` (provisório) | **Sem página dedicada ainda** — Refrigeração tem `status_ads = ads_ativo` mas não é T1 (CLAUDE.md/CPT `aplicacao`); destino ideal seria `/aplicacoes/refrigeracao`, mas essa rota depende do CPT `aplicacao` estar exposto na REST API do WP (mesma pendência do item A.10/Trilha B) e de conteúdo de dor validado (ver `LACUNAS-E-DECISOES-PENDENTES.md`, Notion) |
| DGen Elevadores | `/produtos` (provisório) | Mesma situação de Refrigeração acima — sem página dedicada, mesmo caminho de resolução |
| DGen Industrial | `/produtos` (provisório) | O próprio diagnóstico de origem (`plano-de-acao.md`) já recomendava pausar ou revisar essa campanha (R\$30,73/conv., a menos eficiente das 5) — confirmar com quem administra o Ads se ainda está ativa antes de só trocar a URL |

**Pendência real que isso não resolve:** Refrigeração e Elevadores continuam sem página própria no site novo — trocar a final URL delas por `/produtos` evita 404, mas não é o "página correspondente" pleno que o item pedia. Isso só fecha de verdade quando a página dedicada existir (depende de conteúdo/comercial, não só de código).

---

## 3. Resposta direta: continuo nas páginas ou dou atenção às configurações externas?

**As duas, em paralelo, mas cada trilha tem uma prioridade clara dentro de si.**

Elas não competem pelo mesmo tempo: a Trilha B é, na maior parte, "esperar resposta de alguém" (comercial, quem tem acesso ao WP, quem administra o Vercel), não trabalho contínuo seu. Represar essas perguntas para depois de terminar todas as páginas só atrasa o projeto sem necessidade, porque `WP_API_URL` e a validação da engenharia têm prazo de resposta de terceiros, não de teclado.

Prioridade prática, considerando o que já foi construído:

1. **Disparar a Trilha B inteira agora**, mesmo em paralelo com código: as perguntas de comercial/engenharia/WP/Vercel/GA4 acima. Nenhuma delas depende de mais páginas prontas para ser perguntada.
2. **Dentro da Trilha A, o formulário de orçamento é o item de maior alavancagem imediata** (A.1). O site já tem prova institucional, copy tecnicamente literata, ilustração própria; hoje ele captura zero lead de fato porque não existe um lugar para o visitante submeter uma especificação. É o P0 que sobrevive de 2026-08-05 até hoje.
3. ~~Os itens A.2 a A.7~~ (acessibilidade, placeholder de case, seção Performance, `/aplicacoes/[slug]`, `/blog/[slug]`, sitemap/robots) e o item A.12 (correção de taxonomia de produto) **concluídos** (2026-08-12), e o item A.13 (ajuste do nav) **concluído** (2026-08-13) — eram desenvolvimento puro, sem dependência externa.
4. A.8 (GA4) e A.10 (trocar conteúdo estático por WordPress real) ficam **paradas até a Trilha B confirmar**, respectivamente, `GA4_MEASUREMENT_ID` e `WP_API_URL`. Não adianta escrever o código de integração antes disso além do que já existe em `lib/wordpress.ts` (que já está pronto e esperando).

---

## 4. Fases sugeridas

**Fase 1, agora:** disparar toda a Trilha B (tabela da seção 2) + construir o formulário de orçamento (A.1) + fechar os itens de acessibilidade/copy que não dependem de ninguém (A.2, A.3, A.4).

**Fase 2, quando `WP_API_URL` for confirmado:** validar cada endpoint com `curl | jq` conforme CLAUDE.md, trocar o conteúdo estático das 3 páginas de produto por `lib/wordpress.ts`. `/aplicacoes/[slug]` e `/blog/[slug]` já foram construídos antes desta fase (item A.5/A.6, 2026-08-12) consumindo `lib/wordpress.ts` diretamente — não sobra código de integração pra escrever aqui, só a confirmação da env var (e a correção da taxonomia `categoria_produto` no WP admin, ver Trilha B) pra esse conteúdo parar de devolver 404/vazio.

**Fase 3, quando GA4 e WhatsApp/telefones forem confirmados:** integrar `next/script`/`gtag`, os dois eventos de conversão com UTM, atualizar Footer e `lib/whatsapp.ts` com dados reais.

**Fase 4, pré-go-live:** `sitemap.ts`/`robots.ts`, mapa completo de redirects 301 a partir do Search Console, webhook de revalidação configurado no WP, checklist de "Antes de abrir um PR" do CLAUDE.md rodado em todas as páginas, nova rodada de `/impeccable` sobre a home e as páginas de produto para confirmar que os P0/P1 de 2026-08-05 seguem resolvidos.

**Fase 5, pós-lançamento:** liberação de cases reais (troca do placeholder de Segmentos), ~~expansão da página de indutores quando o brief de engenharia chegar~~ (feito em 2026-09-01, ver Trilha A, item 16 — falta só a faixa técnica para a tabela), iteração de conteúdo de blog.

---

## 5. Bloqueios que impedem o go-live (resumo, atualizado 2026-08-17)

- ~~Nenhum lead-capture funcional existe~~ **Resolvido**: form + `/api/orcamento` + persistência no MongoDB.
- ~~`WP_API_URL` não confirmado~~ **Parcialmente resolvido**: confirmado em produção para posts de blog (item 1.3 acima, verificado ao vivo em `toroid-website.vercel.app/blog`). Produto e Aplicação continuam bloqueados — CPTs `produto`/`aplicacao` retornam 404 na REST API (Trilha B).
- ~~Telefone, e-mail, WhatsApp e redes sociais são placeholders~~ **Resolvido** (Trilha B): dados reais confirmados.
- ~~`GA4_MEASUREMENT_ID` não existe~~ **Resolvido**: propriedade real (`G-XFVM9FCZET`) configurada nas 3 envs da Vercel.
- ~~Mapa de redirects 301 incompleto~~ **Resolvido**: 64 entradas cobrindo o export do Search Console.
- ~~Lighthouse ≥ 90~~ **Validado em produção** (2026-08-17, site real na Vercel).
- ~~Dados técnicos das páginas de produto sem validação formal da engenharia~~ **Resolvido para TC e TP (2026-09-01)**, ver Trilha A, item 16. Os dados de `/aplicacoes/nobreaks` não estavam no escopo dessa revisão e continuam **abertos**.
- Faixa técnica de indutor (indutância, corrente, frequência) para a tabela de especificação de `/indutores-filtros-e-chokes` — o brief de conteúdo chegou e foi aplicado, a faixa numérica continua **aberta**.
- ~~Conteúdo de 4 dos 5 segmentos T1~~ **Atualizado (2026-08-25)**: 3 publicadas de verdade (automação industrial, laboratoriais, integradores), linkadas em `Segmentos.tsx` e em `app/sitemap.ts`. Médicos continua em rascunho (linkada, mas com `robots: noindex`, por ser conteúdo normativo de equipamento regulado). Nenhuma das 4 tem validação formal da engenharia/comercial ainda, ver item 1.5.
- ~~Política de privacidade / checkbox de consentimento LGPD~~ **Resolvido por decisão (2026-08-25)**: mantido só o consentimento básico, sem política completa nem checkbox.
- Formulário técnico por categoria (TC/TP/Indutor) — código pronto e comentado em `lib/orcamento-schema.ts`, aguardando decisão comercial — **ainda aberto**.

Nenhum desses itens é bloqueado por falta de código pronto para escrever, todos dependem de uma resposta de alguém ou de uma decisão de negócio.

### 1.5 Rascunho dos 4 segmentos T1 restantes (2026-08-25, 3 publicadas no mesmo dia)

A pedido explícito ("com base nos dados que temos e conhecimento geral, deixe a base pré-pronta para subirmos depois das páginas institucionais"), as 4 páginas de segmento que faltavam (médicos, automação industrial, laboratoriais, integradores) foram construídas seguindo o mesmo template da página de nobreaks (`AplicacaoHero` + `Prose`/característica→benefício + `PillarChecklist` + `Perguntas`/FAQ + `CTA`), como rotas estáticas irmãs dentro de `app/aplicacoes/`:

- `/aplicacoes/equipamentos-medicos` (ainda rascunho, ver abaixo)
- `/aplicacoes/automacao-industrial`
- `/aplicacoes/equipamentos-laboratoriais`
- `/aplicacoes/integradores-de-sistemas`

Diferenças de conteúdo frente ao template de nobreaks: sem `PillarSpecTable`, porque não existe faixa técnica confirmada para nenhum desses 4 segmentos (nobreaks pelo menos tinha valor do brand book; aqui não há nem isso). O conteúdo técnico (isolação para médicos, imunidade a ruído para automação industrial, estabilidade de medição para laboratoriais, processo de cotação para integradores) vem de conhecimento geral de engenharia aplicado ao que a Toroid já publica sobre construção toroidal, não de dado confirmado pela Toroid para estes segmentos específicos, em nenhuma das 4.

**Atualização (2026-08-26): arte própria em todas as 4**, no mesmo espírito de `NucleoIsolado.tsx`/`BandaRegulacao.tsx` de nobreaks (SVG determinístico, sem estado, animação só CSS, `prefers-reduced-motion` tratado globalmente). Cada emblema de hero afirma visualmente a mesma coisa que o texto ao lado, não é decoração solta: `PulsoProtegido` (médicos, sinal de monitor limpo cruzando a zona contida), `RuidoContido` (automação industrial, ruído de painel que treme perto do limite mas não atravessa), `LeituraEstavel` (laboratoriais, leitura nivelada com escala regular, ghost de deriva quase invisível) e `EspecificacoesSobMedida` (integradores, 3 núcleos de tamanhos diferentes, só o "em cotação agora" gira e tem selo de verificado, porque essa página não tem UM fenômeno elétrico pra ilustrar). Gráfico de corpo correspondente em cada uma: `FaixaIsolamento` (corte de isolação com a folga rotulada), `FaixaControle` e `FaixaEstabilidade` (mesma técnica de onda dupla de `BandaRegulacao.tsx`, retemadas para ruído de painel e deriva de instrumento) e `FluxoEspecificacao` (as 4 etapas do processo de especificação, não uma onda, porque a dor de abertura de integradores é de processo, não de característica elétrica).

**Atualização, mesmo dia**: a pedido ("já coloque as seções de segmentos que estão pendentes também"), 3 das 4 foram publicadas de verdade: linkadas nos respectivos cards de `components/sections/Segmentos.tsx` (que antes só tinha link em nobreaks) e adicionadas a `app/sitemap.ts`, sem mais `robots: noindex`. **Equipamentos médicos ficou de fora dessa publicação, por decisão própria, não por pedido do usuário**: a página já tem cuidado redobrado nas referências normativas (família IEC 60601, tratada como algo a confirmar com engenharia, nunca como certificação já obtida, mesmo motivo que levou nobreaks a usar "considerando os requisitos de" em vez de "ensaiada conforme", ver item A.5 na Trilha A), mas por ser conteúdo normativo de equipamento regulado sem validação da engenharia, manteve o `robots: noindex` e ficou de fora do sitemap mesmo assim. Ela está linkada no card "Fabricantes de equipamentos médicos" de `Segmentos.tsx` (link interno não depende de indexação), então é navegável a partir da home, só não é indexável nem lista no sitemap. **Antes de indexar médicos**: engenharia confirma isolação/parâmetros reais, comercial confirma se a dor de abertura escrita é a real.
